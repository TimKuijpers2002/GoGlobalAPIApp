import React from "react";
import {Sidebar} from "../Components/Sidebar";

export default function LocationSubmit() {
    return (
        <Sidebar data={
            <div>
                Hello location submits!
            </div>
        }/>
    );
}